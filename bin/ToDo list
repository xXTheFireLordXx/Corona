Do to:
Rmove table Noun -rename to Nouns and fix word updater to add only words not exsiting in the table --- recreated 1/13/2016
Create function called userinput check...

add basic functions to help with sqlite creation
    This includes 
        Table creation ---1/13/2016
        Table removale --1/13/2016 
        Table calling what was this? Was it print tables?
        Table editing ( only can add columns with built in functions of sqlite have to make a work around for deleteing colums.... back up-copy all colums that you want-delete old table-rename to old table name.) 
        row editing
        row creation
        Search tables
        Call_rows rename as check_rows? mutate code from update to be a new function called check_rows
        
        
Create funtion for user input 
    Needs to have:
        quit function --1/16
        looping ablitlies.... could work if all cmds would call this funtion at the end of their work... allows for next comand to work. --1/16
        
Create Basic String/List Analizer
    Neededs to do:
        analize sentance structer... (plit into list then analize words in list)
        reconize key words/action EX: run, pause, resume, quit.... ect ect
        
This is all for basic basic skeleton fram work for Corona